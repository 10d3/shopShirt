import React from "react";

//want to make a beautifull dashboard with sidebar using params to switch between pages
export default function page(props: { params: Promise<{ page: string }> }) {
	return <div>page</div>;
}
